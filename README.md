# setup-isolated-lan

## Usage

```bash
sudo ./setup-isolated-lan.sh
```

## Notes

- If not working and you are using older Ubuntu versions, change 'bind-dynamic' to 'bind-interfaces' in the script.
